apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: optimus-ingress
spec:
  rules:
    - host: optimus.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-service
                port:
                  number: 80
